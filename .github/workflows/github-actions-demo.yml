

name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      run: apt-get update && apt-get install -y git zsh vim tmux curl cmake build-essential python3-dev
      run: bash ./setup.sh
